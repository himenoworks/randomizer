const Dialog = () => {
   return (
      <dialog className="z-50 top-0 left-0 w-full h-full backdrop:bg-opacity-50 open:animate-fade-in backdrop-blur-sm bg-black/30"></dialog>
   );
};

export default Dialog;
